% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outage.R
\name{get_outage_rtm_matches}
\alias{get_outage_rtm_matches}
\title{Match full generator outages to RTM bid gaps}
\usage{
get_outage_rtm_matches(
  rtm_data,
  outages,
  generators = NULL,
  edge_minutes = 5,
  min_matches = 1L,
  resolve_collisions = TRUE,
  return_detail = FALSE
)
}
\arguments{
\item{rtm_data}{RTM bid data. Must include \code{MARKETPRODUCTTYPE},
\code{RESOURCEBID_SEQ}, \code{SCH_BID_XAXISDATA}, \code{SCH_BID_TIMEINTERVALSTART},
\code{SCH_BID_TIMEINTERVALSTOP}, \code{TIMEINTERVALSTART}, and \code{TIMEINTERVALEND}.}

\item{outages}{CAISO prior trade day outage data.}

\item{generators}{Character vector of resource IDs, passed to
\code{\link[=get_generator_outages]{get_generator_outages()}}.}

\item{edge_minutes}{Slack in minutes when aligning outage times to bid gaps
(default 5).}

\item{min_matches}{Minimum number of matched outages.}

\item{resolve_collisions}{If \code{TRUE}, resolve collisions where the same
\code{RESOURCEBID_SEQ} is claimed by multiple named resources.}

\item{return_detail}{If \code{TRUE}, return final matches, match detail, scores,
raw matches, and cleaned outages.}
}
\value{
If \code{return_detail = FALSE}, a tibble of generator-bid ID pairs. If
\code{return_detail = TRUE}, a named list with \code{final_matches}, \code{match_detail},
\code{scores}, \code{raw_matches}, and \code{outages}.
}
\description{
When a generator goes offline, its bid curve disappears. This function finds
gaps in RTM bid submissions which align with reported outage windows, linking
outage records to bid IDs.
}
\details{
Each bid's time interval, deduplicated. The RTM parquet has two sets of
timestamp columns depending on the record type, so we coalesce to handle both.

Find gaps between consecutive bids for each ID. A gap is where one bid ends
and the next one starts -- the generator wasn't bidding during that window,
which likely means it was offline.

The highest MW a bid ID ever offers -- this is our best guess at that ID's
rated capacity, used later to check if it matches the outage record's PMAX.

Join outages to bid gaps: the gap's \code{last_bid_stop} should be near the outage
start (within \code{edge_minutes}), and the gap's \code{next_bid_start} should be at or
after the outage end. This pins an outage to a specific bid ID that went quiet
at the right time.

For each generator-ID pair, aggregate across all matched outages. Score each
(\code{generator}, \code{RESOURCEBID_SEQ}) pair using a combined metric that rewards
both more matches and lower MW error. Compute a confidence ratio comparing the
top candidate's score to the runner-up, to flag ambiguous matches. Resolve
collisions where the same \code{RESOURCEBID_SEQ} is claimed by multiple named
resources -- keep only the highest-confidence claim.
}
